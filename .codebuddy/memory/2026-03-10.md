# 2026-03-10

## 修复 skills/ 目录自引用符号链接（无限递归嵌套）

- 问题：`skills/web3-butler/web3-butler`、`skills/web3-market/web3-market`、`skills/private-fork-dev/private-fork-dev` 三个符号链接指向各自的父目录，造成无限递归嵌套。
- 根因：commit `234cde7f4` 引入了这些自引用链接，不是 `sync-codebuddy.sh` 导致的（该脚本只在 `.codebuddy/skills/` 创建链接）。
- 修复：删除了这 3 个自引用符号链接。git 显示 3 个 deletion，需要提交。
