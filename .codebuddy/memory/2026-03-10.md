# 2026-03-10

## 修复 skills/ 目录自引用符号链接（无限递归嵌套）

- 问题：`skills/web3-butler/web3-butler`、`skills/web3-market/web3-market`、`skills/private-fork-dev/private-fork-dev` 三个符号链接指向各自的父目录，造成无限递归嵌套。
- 根因：commit `234cde7f4` 引入了这些自引用链接，不是 `sync-codebuddy.sh` 导致的（该脚本只在 `.codebuddy/skills/` 创建链接）。
- 修复：删除了这 3 个自引用符号链接。git 显示 3 个 deletion，需要提交。

## Web3 Market 2.0 架构演进文档审核与重写

- 文件：`skills/web3-market/plans/SUI_FIRST_ZERO_BASE64.md`
- 审核发现 4 个致命问题：(1) P1 未锚定真实代码路径（缺少改造文件清单）；(2) P2 Sui-First 无落地锚点（代码库 Sui 引用为零）；(3) P3 对账/审计忽视已有 ReconciliationSummary + AuditAnchor 实现；(4) 争议窗口忽视已有 6 步 dispute handler。
- 重写后文档包含：代码审计基线表、URI 协议定义、DeliveryPayload 类型扩展方案、改造文件清单（每阶段）、Move 合约结构设计、Merkle 批次锚定引擎、里程碑依赖图、风险评估矩阵。
- 关键代码锚点：`resources/tools.ts:163-168`(bytesBase64 schema)、`resources/http.ts:851,955`(base64编解码)、`media.ts:233`(loadWebMediaInternal 协议分支)、`delivery.replies.ts:257`(deliverMediaReply)、`lease.ts`(459L)、`settlement.ts`(867L)、`dispute.ts`(521L)。
