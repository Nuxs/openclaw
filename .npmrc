# pnpm build-script allowlist lives in package.json -> pnpm.onlyBuiltDependencies.

# Prefer local workspace packages over fetching from an npm registry.
# This avoids 404s when a mirror doesn't have private/internal packages.
link-workspace-packages=true
prefer-workspace-packages=true

# Keep the default registry (possibly a mirror), but ensure @openclaw scope can resolve.
@openclaw:registry=https://registry.npmjs.org/
