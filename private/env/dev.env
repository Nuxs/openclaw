# ============================================================================
# OpenClaw 私有化 — 开发环境 (dev)
# ============================================================================
# 复制到项目根 .env 或 ~/.openclaw/.env 即可使用
# 加载优先级: process env > ./.env > ~/.openclaw/.env > openclaw.json env 块

# -- Gateway ------------------------------------------------------------------
OPENCLAW_GATEWAY_TOKEN=dev-token-change-me
# OPENCLAW_GATEWAY_PASSWORD=
OPENCLAW_STATE_DIR=~/.openclaw
# OPENCLAW_CONFIG_PATH=~/.openclaw/openclaw.json

# -- 模型 API（至少填一个）----------------------------------------------------
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GEMINI_API_KEY=...
# OPENROUTER_API_KEY=sk-or-...

# -- 渠道（按需启用）----------------------------------------------------------
# TELEGRAM_BOT_TOKEN=123456:ABCDEF...
# DISCORD_BOT_TOKEN=...
# SLACK_BOT_TOKEN=xoxb-...
# SLACK_APP_TOKEN=xapp-...

# -- Docker -------------------------------------------------------------------
OPENCLAW_IMAGE=openclaw:dev
# 如果 Docker 构建提示 pnpm lockfile 版本不兼容，可临时开启
OPENCLAW_PNPM_FORCE=1
OPENCLAW_CONFIG_DIR=~/.openclaw
OPENCLAW_WORKSPACE_DIR=~/.openclaw/workspace
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790
OPENCLAW_GATEWAY_BIND=lan
# 仅 dev 使用：避免首次部署因缺少配置而反复重启
OPENCLAW_ALLOW_UNCONFIGURED=1

# -- 开发专用 -----------------------------------------------------------------
NODE_ENV=development
LOG_LEVEL=debug
