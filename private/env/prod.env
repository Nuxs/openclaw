# ============================================================================
# OpenClaw 私有化 — 生产环境 (prod)
# ============================================================================
# 重要: 真实 token/key 不要提交到 Git，通过 CI secrets 或密钥管理系统注入

# -- Gateway ------------------------------------------------------------------
OPENCLAW_GATEWAY_TOKEN=4092cf69415bc770d52c1556177a82136e355f811ccacec52fd69b443c804f54
OPENCLAW_STATE_DIR=/data/.openclaw
OPENCLAW_WORKSPACE_DIR=/data/workspace

# -- 模型 API -----------------------------------------------------------------
# OPENAI_API_KEY=       # 从 CI secrets 注入
# ANTHROPIC_API_KEY=    # 从 CI secrets 注入

# -- 渠道 ---------------------------------------------------------------------
# TELEGRAM_BOT_TOKEN=   # 从 CI secrets 注入
# DISCORD_BOT_TOKEN=    # 从 CI secrets 注入

# -- Docker -------------------------------------------------------------------
OPENCLAW_IMAGE=ghcr.io/your-org/openclaw:latest
OPENCLAW_CONFIG_DIR=/data/.openclaw
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790
OPENCLAW_GATEWAY_BIND=lan

# -- 环境 ---------------------------------------------------------------------
NODE_ENV=production
LOG_LEVEL=warn
